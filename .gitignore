# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# storybook
/storybook-static/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# storybook
/storybook-static
debug-storybook.log

# vouchers-modelo preenchidos: têm dados reais de clientes (nome, e-mail,
# telefone) e o repositório é público. Ficam só na máquina.
/Voucher*.pdf
/VOUCHER*.pdf

# export do design system (Figma): ~71MB de binários e fontes licenciadas.
# Fonte de verdade do design, não do app — fica fora do repositório público.
/Mamut Trekking Design System-vs02/
