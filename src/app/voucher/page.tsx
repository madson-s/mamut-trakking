import { VoucherStudioLoader } from '@/components/voucher/VoucherStudioLoader';

// Rota estática que monta o estúdio (carregado só no cliente).
export default function VoucherPage() {
  return <VoucherStudioLoader />;
}
